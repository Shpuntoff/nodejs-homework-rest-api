const { getAll, getById, add, removeById, updateById, updateFavoriteById } = require("./contactsControllers");

module.exports = {
  getAll,
  getById,
  add,
  removeById,
  updateById,
  updateFavoriteById
};
